import React from 'react';

function App() {
  return (
    <main style={{ fontFamily: 'Inter, system-ui, sans-serif', padding: '2rem', maxWidth: 720, margin: '0 auto' }}>
      <h1>KohoReach MVP</h1>
      <p>
        The app shell is loading correctly. Route modules were referenced in this branch but not included in the
        repository snapshot, so this fallback keeps the deployment build healthy.
      </p>
    </main>
  );
}

export default App;
